{
  "name": "streamtape-direct-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to extract and instantly redirect to fresh Streamtape CDN streams",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "keywords": ["cloudflare-worker", "streamtape", "stream-resolver", "video-cdn"],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.100.0"
  }
}
